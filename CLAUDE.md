# CLAUDE.md — project-hyeop

이 저장소의 git 관련 모든 작업(push, PR, `gh` CLI 등)은 GitHub 계정 **`ruletheaiworld`** 로 수행한다. remote URL 에 `ruletheaiworld@` 가 박혀 있어 활성 계정과 무관하게 자동 적용.

폴더 구조·작업 메모리 위치는 `docs/CLAUDE.md` 참조.
